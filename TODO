#understand using flask

        - As far as i understand now it is made for server side usage

#test the whole project

    server side:
        -read about PhantomJS
        -allow only receiving song's name and duration
        -permit only GET message